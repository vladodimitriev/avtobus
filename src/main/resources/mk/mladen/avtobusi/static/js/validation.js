function validation1(errorMessage) {
	$("#departurePlace")[0].setCustomValidity(errorMessage);
}

function validation2(errorMessage) {
	$("#destinationPlace")[0].setCustomValidity(errorMessage);
}

function validation(errorMessage) {
	$("#departurePlace")[0].setCustomValidity(errorMessage);
}
